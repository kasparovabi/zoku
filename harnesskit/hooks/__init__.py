"""HarnessKit hooks — native Claude Code hook integration.

This subpackage provides shell scripts and a Python enforcement engine
that run as Claude Code hooks (PreToolUse, PostToolUse, SessionStart).

When installed, HarnessKit becomes a native security layer inside
Claude Code rather than an external tool.
"""
